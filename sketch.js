function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  resizeCanvas(windowWidth, windowHeight)
  background(51);
  fill(255);
  textSize(100);
  text("Hi", width/2, 20, 50);
}
